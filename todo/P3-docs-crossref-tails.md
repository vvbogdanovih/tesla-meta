# Хвости в доках: ADR-0008 без monopay, застарілі перехресні посилання

- **Пріоритет:** P3 — документація
- **Репозиторій:** tesla-meta
- **Статус:** TODO

## Проблема

1. **ADR-0008**: заголовок `# ADR-0008: … (IBAN / LiqPay)`, рядок у `docs/adr/README.md:23` і CLAUDE.md («0008 — … канали IBAN/LiqPay») — без monopay (канал `card` через monopay існує з ADR-0015).
2. **CLAUDE.md:17**: `adr/ # … (0000-template + 0001..0006)` — реально вже 0001..0017.
3. **ADR-0011, «Пов'язано»**: посилається на неіснуючий FRD §3.12 — має бути §3.3a (FR-2.8–2.13).
4. **FRD §6**: `product_images` без `is_live`; блоку `wishlist_items` немає; у примітці (рядок ~376) привид старої архітектури `AND system_id = :sys`.
5. **Версії**: `docs/README.md` каже «Draft v1.0» для PRD/FRD — реально FRD v1.2.

## Що зробити

Виправити 5 пунктів; версії/дати підняти разом із наступним док-комітом. **Коміт — лише з явної згоди користувача.**

> Початкова передумова файлу (два незакомічені change-sets «Monopay+ліди» / «wishlist+price-sheet+isLive») зникла — усе закомічено; FRD §8 п.1 уже виправлено («Вирішено: monopay»). Лишились тільки хвости вище.
