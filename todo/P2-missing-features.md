# Незавершені/відсутні функції (розділи адмінки, юр-сторінки, фічі)

- **Пріоритет:** P2 (окремі пункти — Фаза 2 за дорожньою картою)
- **Репозиторії:** tesla-admin, tesla-frontend, tesla-backend
- **Статус:** TODO (виявлено 17.07.2026)

## 1. Розділ «Користувачі» в адмінці — заглушка ComingSoon
`tesla-admin/src/app/(dashboard)/users/page.tsx:4`. Керування ролями user/admin/superadmin наразі неможливе з UI — лише через БД/бекенд. Найважливіший із заглушок (RBAC).

## 2. Управління банерами — заглушка ComingSoon (FR-A7, FR-1.1)
`tesla-admin/.../banners/page.tsx:4`. Модель `Banner` є, API/UI немає → банер головної (FR-1.1, керований з адмінки) **не працює end-to-end**.

## 3. Блог — заглушка ComingSoon + відсутній фронт (FR-A6, FR-9.x, D1)
`tesla-admin/.../blog/page.tsx:4`; на фронті немає `/blog` чи `/blog/[slug]`, на бекенді немає `/api/blog`. Модель `BlogPost` є. (Фаза 2.)

## 4. Юридичні сторінки відсутні (P17–P20)
Немає `/delivery`, `/returns`, `/privacy`, `/offer`. FR-F2 (футер) і checkout («умови оферти») на них посилаються. Оферта/приватність юридично важливі для еквайрингу.

## 5. Відновлення пароля відсутнє в UI (FR-7.5)
Бекенд-константи `AUTH.FORGOT_PASSWORD/RESET_PASSWORD` є, але `auth.api.ts` їх не експонує, немає сторінок forgot/reset, немає «Забули пароль?» на логіні. Бекенд-ендпоінти теж не підтверджені — перевірити.

## 6. Price range slider у фільтрах відсутній (FR-2.4)
Бекенд приймає `minPrice/maxPrice`, фронт читає їх з URL, але UI-контрола (слайдера) в `CatalogFilters.tsx` немає.

## 7. Нотифікації лідів менеджеру відсутні (FR-8.3, FRD §8 #6)
Ліди лише пишуться в БД. Немає email/Telegram-сповіщень менеджеру. Email-модуль було прибрано з бекенду (`a69fe53`) — жодних нотифікацій про замовлення/ліди/оплату. Якщо це свідоме рішення для MVP — зафіксувати; інакше прогалина UX.
